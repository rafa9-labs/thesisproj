# Quant Application Standards
- **Vectorization**: Prioritize NumPy/Pandas vectorization over loops to reduce computation.
- **Decoupling**: Separate Feature Engineering (Math) from the Backtester (Execution).
- **Safety**: Use Pydantic for configuration validation (e.g., CLASS_DEFAULTS).